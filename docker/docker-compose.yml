version: "3.8"
services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
      - "8080:8080"
    environment:
      - PORT=8080
      - LOG_LEVEL=info
    volumes:
      - ./configs:/app/configs
      - ./data:/app/data
    restart: unless-stopped
